# To-Do-List-Application
A C++ To-Do List application using vectors to store tasks. Features an interactive menu with options to add tasks with descriptions, view numbered task lists, and remove completed tasks by number. Input validation ensures proper task selection, while dynamic memory management allows unlimited tasks. Clean interface with success/error messages.
